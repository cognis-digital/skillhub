---
name: web-fetch
version: 1.0.0
description: Fetch the contents of a URL over HTTP(S).
author: skillhub-demo
tags: [http, network, io]
---

# web-fetch

Given a URL, fetch its body and return the text. Use this as a building block
for any skill that needs to read from the web.

## Usage

Call with a single `url` argument. Returns the decoded response body.
